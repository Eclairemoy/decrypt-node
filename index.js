// `data` is the data you encrypted and passed into `evervault.run` from your server. The Function 
// automatically decrypts the data and returns it.
exports.handler = async (data, context) => {
    return data
};
